<div class="container">
  <div class="progress-bar progress-bar--animated">
    <div class="progress-bar__track">
      <div class="progress-bar__indicator progress-bar__indicator--90"></div>
    </div>
  </div>
  <h1>Dernière étape ! 💪</h1>
  <h4>Créez votre compte pour accéder à votre espace HappyTasks</h4>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "form" }) do |f| %>
    <div class="enrollment__form__group">
      <div class="form__group">
        <div class="form__field">
          <%= f.label :first_name, "Votre prénom", class: "form__label" %>
          <%= f.text_field :first_name, class: "form__input #{resource.errors[:first_name].any? ? 'form__input--error' : ''}" %>
          <% if resource.errors.include?(:first_name) %>
            <p class="form__error"><%= resource.errors[:first_name].first %></p>
          <% end %>
        </div>
        <div class="form__field">
          <%= f.label :email, "Votre email", class: "form__label" %>
          <%= f.email_field :email, class: "form__input #{resource.errors[:email].any? ? 'form__input--error' : ''}" %>
          <% if resource.errors.include?(:email) %>
            <p class="form__error"><%= resource.errors[:email].first %></p>
          <% end %>
        </div>
        <div class="form__field">
          <%= f.label :password, "Choisissez un mot de passe", class: "form__label" %>
          <%= f.password_field :password, class: "form__input #{resource.errors[:password].any? ? 'form__input--error' : ''}" %>
          <% if resource.errors.include?(:password) %>
            <p class="form__error"><%= resource.errors[:password].first %></p>
          <% end %>
        </div>
        <div class="form__field">
          <%= f.label :password_confirmation, "Confirmez votre mot de passe", class: "form__label" %>
          <%= f.password_field :password_confirmation, class: "form__input #{resource.errors[:password_confirmation].any? ? 'form__input--error' : ''}" %>
          <% if resource.errors.include?(:password_confirmation) %>
            <p class="form__error"><%= resource.errors[:password_confirmation].first %></p>
          <% end %>
        </div>
      </div>
    </div>
    <div class="form__end">
      <%= f.submit "Créer mon compte", class: "form__button" %>
    </div>
  <% end %>
</div>